document.addEventListener('DOMContentLoaded', () => {
  const tracker_form = document.querySelector('.js-mood-error-message');
  if (!tracker_form) {
    return
  };

  const dateField = document.querySelector('input[name="date"]');
  
});